diff --git a/third_party/application-services/components/remote_settings/src/client.rs b/third_party/application-services/components/remote_settings/src/client.rs
index b951d45c0a..9ec59449d8 100644
--- a/third_party/application-services/components/remote_settings/src/client.rs
+++ b/third_party/application-services/components/remote_settings/src/client.rs
@@ -388,12 +388,19 @@ impl<C: ApiClient> RemoteSettingsClient<C> {
             // Retry with packaged dataset as base
             self.reset_storage()?;
             self.perform_sync_operation()?;
-            // Verify signature again
-            self.verify_signature().inspect_err(|_| {
-                // And reset with packaged data if it fails again.
-                self.reset_storage()
-                    .expect("Failed to reset storage after verification failure");
-            })?;
+            // Verify signature again. A second failure must not abort the
+            // process: reset_storage used to .expect() here, which is
+            // MozCrashReason=explicit panic on first content navigation
+            // (urlbar → SearchService / rust Remote Settings sync).
+            if let Err(e) = self.verify_signature() {
+                if let Err(reset_err) = self.reset_storage() {
+                    debug!(
+                        "{0}: reset after signature failure also failed: {1}",
+                        self.collection_name, reset_err
+                    );
+                }
+                return Err(e);
+            }
         }
         trace!("{0}: sync done.", self.collection_name);
         Ok(())
